# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling
.pytest_cache/
.mypy_cache/
.coverage

# Local device config (never commit per-device secrets)
config/agent.toml
*.local.toml

# ./run generated files
.run/

# Per-machine state for the discussion-mirror hook (the log itself IS committed)
.claude/hooks/.last-mirrored
