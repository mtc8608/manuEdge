# manuEdge — Discussion Log

Shared, append-only record of project discussions with Claude Code. Each entry is
one turn: the prompt and Claude's final response. Written automatically by the
`Stop` hook (`.claude/hooks/mirror-discussion.sh`) and committed to the repo so
the whole team has the context.

Edit or prune freely — it's a normal Markdown file.
